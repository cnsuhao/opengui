Building the examples requires that both OpenGUI and the OpenGL
reference renderer (OGLRenderer) have already been successfully
built.

This holds true for both build configurations (Debug and Release).

